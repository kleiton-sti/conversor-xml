A nova planilha continha mais campos em relação a outra, foi preciso mudar as referencias em linha de conversão;

A nova planilha contém cabeçalho, o código atual já interpreta a primeira linha como dados a ser inseridos (exclui cabeçalho);

É preciso que na pasta do excel, os dados a ser convertidos estejam na primeira planilha.

Na planilha, percebi que o cód da entidade prevista e o código cargo estavam discrepante do exemplo do arquivo da AUDESP. (troquei na planilha)